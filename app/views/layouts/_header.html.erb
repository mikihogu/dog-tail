<header class="sticky-top">
  <nav class="navbar navbar-expand-lg navbar-light">
    <div class="container">
      <% if request.path.match(/\/admin(\/)?/) %>
        <a class="navbar-brand" href="/admin"><%= image_tag 'h-logo.png', size: '65x50' %></a>
      <% else %>
        <a class="navbar-brand" href="/"><%= image_tag 'h-logo.png', size: '65x50' %></a>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">

        <% if admin_signed_in? %>
          <li>
            <%= link_to admin_posts_path, class:"a" do %>
              <div class="nav-link a-header">施設一覧</div>
            <% end %>
          </li>
          <li>
            <%= link_to admin_members_path, class:"a" do %>
              <div class="nav-link a-header">会員一覧</div>
            <% end %>
          </li>
          <li>
            <%= link_to admin_tags_path, class:"a" do %>
              <div class="nav-link a-header">タグ一覧</div>
            <% end %>
          </li>
          <li>
            <%= link_to admin_categories_path, class:"a" do %>
              <div class="nav-link a-header">カテゴリー一覧</div>
            <% end %>
          </li>
          <li>
            <%= link_to destroy_admin_session_path, method: :delete, class:"a" do %>
              <div class="nav-link a-header">ログアウト</div>
            <% end %>
        </li>

        <% elsif request.path.match(/\/admin(\/)?/) %>
          <li>
            <%= link_to new_admin_session_path, class:"a" do %>
              <div class="nav-link a-header">ログイン</div>
            <% end %>
          </li>

        <% elsif member_signed_in? %>
          <li>
            <%= link_to about_path, class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-dog"></i><small> このサイトについて</small></div>
            <% end %>
          </li>
          <li>
            <%= link_to member_path(current_member), class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-user"></i><small> マイページ</small></div>
            <% end %>
          </li>
          <li>
            <%= link_to posts_path, class:"a", data: { 'turbolinks': false } do %>
              <div class="nav-link a-header"><i class="fas fa-map-marked-alt"></i><small> 投稿一覧</small></div>
            <% end %>
          </li>
          <li>
            <%= link_to post_interests_path(current_member), class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-paw"></i><small> ブックマーク一覧</small></div>
            <% end %>
          </li>
          <li>
            <%= link_to destroy_member_session_path, method: :delete, class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-sign-out-alt"></i><small> ログアウト</small></div>
            <% end %>
          </li>

        <% else %>
          <li>
            <%= link_to about_path, class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-dog"></i><small> このサイトについて</small></div>
            <% end %>
          </li>
          <li>
            <%= link_to posts_path, class:"a" do %>
              <div class="nav-link a-header"><i class="fas fa-map-marked-alt"></i><small> 投稿施設一覧</small></div>
            <% end %>
          </li>
          <!--<li>-->
          <!--  <%#= link_to new_member_registration_path, class:"a" do %>-->
          <!--    <div class="nav-link a-header"><i class="fas fa-user"></i> 新規登録</div>-->
          <!--  <%# end %>-->
          <!--</li>-->
          <!--<li>-->
          <!--  <%#= link_to new_member_session_path, class:"a" do %>-->
          <!--    <div class="nav-link a-header"><i class="fas fa-sign-in-alt"></i> ログイン</div>-->
          <!--  <%# end %>-->
          <!--</li>-->
        <% end %>
        </ul>
      </div>
    </div>
    <div>
      <% if admin_signed_in? %>
        <%= render 'layouts/admin_search' %>
      <% elsif member_signed_in? %>
        <%= render 'layouts/search' %>
      <% end %>
    </div>
  </nav>
</header>