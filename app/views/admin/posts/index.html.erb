<div class="container my-3">
  <div class="row">
    <div class="col-md-8">
      <h2 class="m-3 border-bottom"><i class="fas fa-dog"></i> 施設一覧</h2>
    </div>
  </div>

  <table class="table">
    <thead>
      <tr>
        <th>投稿者</th>
        <th>施設名称</th>
        <th>所在地</th>
        <th>一言紹介文</th>
        <th></th>
        <th>更新日</th>
        <th>投稿日</th>
      </tr>
    </thead>
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td>
            <%= link_to admin_member_path(post.member) do %>
              <%= post.member.nickname %>
            <% end %>
          </td>
          <td>
            <%= link_to admin_post_path(post) do %>
              <%= post.name %>
            <% end %>
          </td>
          <td><%= post.prefecture %></td>
          <td><%= post.introduction %></td>
          <td>
            <span><i class="fas fa-thumbs-up" style= "color:gray;"></i> <%= post.favorites.count %></span>
            <span><i class="fas fa-star" style= "color:gray;"></i> <%= post.interests.count %></span>
            <span><i class="fas fa-comment-dots" style= "color:gray;"></i> <%= post.comments.count %></span>
            <span><i class="fas fa-exclamation"></i> </span>
          </td>
          <td><%= post.updated_at %></td>
          <td><%= post.created_at %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <div>
    <%= render 'public/posts/map_index', posts: @posts %>
  </div>

</div>